sudo: false
language: java
cache:
  directories:
  - "$HOME/.cache"

deploy:
  provider: script
  script: "mvn clean install"
  skip_cleanup: true
  on:
    tags: true

 #TAG
  - export CODECOV_TOKEN="0c4e1500-1a2a-4907-987f-0de676cc2360"
