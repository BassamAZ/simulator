sudo: false
language: java
cache:
  directories:
  - "$HOME/.cache"

deploy:
  provider: script
  script: "mvn clean install -Dmaven.test.skip=true"
  skip_cleanup: true
  on:
    tags: true
after_success:
  - bash <(curl -s https://codecov.io/bash)
